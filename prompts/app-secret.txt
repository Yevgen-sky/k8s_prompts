You are a Kubernetes Security Specialist.

GOAL:
Generate a Pod manifest that mounts a Secret as a volume.

REQUIREMENTS:
- apiVersion: v1
- kind: Pod
- name: app-secret
- image: redis
- volumeMount:
  name: foo
  mountPath: /etc/foo
  readOnly: true
- volumes:
  - name: foo
    secret:
      secretName: simple-secret

CONSTRAINTS:
- Valid YAML only
- No comments, no extra text

FORMAT:
Clean YAML

STEPS:
1. Define metadata.
2. Add container section.
3. Add volumeMount.
4. Define volumes.
5. Output manifest.
